---
# Required for FileDump* and LoadGFF3 pipelines
"eg-file-io":
    version: 3
    deps:
        - "formula": "fontconfig"
          "options": ["without-docs"]
        - "formula": "homebrew/science/genometools"
          "options": ["without-pangocairo"]
        - "ensembl/ensembl/maftools"
        - "homebrew/science/newick-utils"
